/*
 * NewsList Messages
 *
 * This contains all the text for the NewsList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NewsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewsList component!',
  },
});
