// import { fromJS } from 'immutable';
// import { selectFinancialWizardPortfolioDomain } from '../selectors';

describe('selectFinancialWizardPortfolioDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
