// import { fromJS } from 'immutable';
// import { selectFinancialWizardContainerDomain } from '../selectors';

describe('selectFinancialWizardContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
